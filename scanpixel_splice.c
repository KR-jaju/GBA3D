
#include "gpi_private.h"

void	scanpixel_splice(t_scanpixel *self, t_pixel *dst) {
	i32 const	x = self->x;
	i32 const	dst_z = dst->z;
	t_pixel		*src = self->global;
	t_pixel		*next;

	while (src != NULL && (src->))

}